---
id: 3D-02-drei-orbitcontrols
name: drei OrbitControls
category: 3d
description: drei OrbitControls, user drag/zoom to orbit 3D scene
license: MIT (drei)
---

usage_sectors: [product-3d, configurator, immersive-3d, architecture-viewer]
forbidden_sectors: [auto-camera-only]

forbidden_with: []

requires:
  libraries: ["@react-three/fiber", "@react-three/drei"]

reference_impl:
  - file: Mobilyacı/3d-demo/research/drei-advanced-visual-components-research.md

preview_snippet: |
  import { OrbitControls } from '@react-three/drei';
  <OrbitControls
    enableZoom={true}
    enablePan={false}
    minPolarAngle={Math.PI / 3}
    maxPolarAngle={Math.PI / 2}
    autoRotate={true}
    autoRotateSpeed={0.4}
  />

research_refs:
  - 3D-TECHSTACK.md
