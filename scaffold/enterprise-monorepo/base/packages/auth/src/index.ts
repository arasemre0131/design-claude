/**
 * @preset/auth — barrel export
 * Server-only code'u burada re-export ETMIYORUZ — "use server"/"use client"
 * sinirini korumak icin /server veya /client ile import edilmeli.
 */
export type { AuthUser } from "./server";
