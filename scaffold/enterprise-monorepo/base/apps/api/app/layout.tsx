/**
 * apps/api — Next.js minimal layout.
 * Bu app sadece Route Handler sunar; UI yok.
 */
export default function ApiRootLayout({ children }: { children: React.ReactNode }) {
  return children as React.ReactElement;
}
